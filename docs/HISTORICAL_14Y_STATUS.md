Implementation status: backend engine added behind an opt-in query on the existing spatial-index function. Production dashboard behavior is unchanged until calibration passes. Preview build is READY after avoiding the Hobby-plan serverless-function count limit by keeping the engine in a shared library rather than a new API function.

Source-audit progress (2026-08-26): the newer GISTDA 2011-2020 recurrence layer does not return Chiang Mai features by province code, so it must not be treated as universal nationwide coverage. A second GISTDA recurrence layer for 2005-2016 has direct province/district fields and year flags and is being audited for 2011-2016 coverage. Its older ArcGIS query interface rejects pagination parameters, so the hidden audit route now uses a legacy-compatible distinct-value query. The 5-year production dashboard remains unchanged while this 14-year source calibration continues.